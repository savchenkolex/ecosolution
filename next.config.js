/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    output: 'export',
    basePath: '/ecosolution',
    images: {
        unoptimized: true
    }

}

module.exports = nextConfig
